import React from 'react';
import PropTypes from 'prop-types';
import "./styles.css"

Image.propTypes = {
    
};

function Image(props) {
    const {image} = props;
    return (
        <div>
            
        </div>
    );
}

export default Image;